services:
  awg-manager:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    command: flask --app webapp run --host=0.0.0.0 --port=8000 --debug
